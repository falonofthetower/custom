<div class="row">
  <div class="span8 well">
      <p class="content"><%= post.content %></p>
      <hr class="black"/>
      <ul>
        <li>
          <%= image_tag avatar_url(post.user) %> <%= link_to post.user.email, user_path(post.user) %>
        </li>
        <li>
          <small>on <%= post.created_at %></small>
        </li>
      </ul>
  </div>
</div>
<p>
  <ul id="replies">
    <% post.replies.each do |reply| %>
      <li>
        <%= render 'shared/show_reply', reply: reply, user: reply.user, post: reply.post %>
      </li>
    <% end %>
  </ul>

  <div class="well" id="reply_form">
    <%= render 'replies/form', reply: @reply, post: post, reply_errors: @reply_errors %>
  </div>
</p>
