<tr>
  <td><%= link_to post.title, post_path(post) %></td>
  <td><%= image_tag avatar_url(post.user) %>  <%= link_to post.user.email, user_path(post.user) %></td>
  <td><%= post.created_at %></td>
</tr>
